import { requireAdmin } from '@/lib/auth';import { db } from '@/lib/db';import { Shell } from '@/components/shell';
export const dynamic='force-dynamic';
export default async function Reports(){const user=await requireAdmin();const [users,courses,completed,attempts,submissions,certificates]=await Promise.all([db.user.count(),db.course.count(),db.enrollment.count({where:{completedAt:{not:null}}}),db.quizAttempt.aggregate({_avg:{score:true},_count:true}),db.assignmentSubmission.groupBy({by:['status'],_count:true}),db.certificate.count()]);return <Shell user={user}><div className="page-title"><p className="eyebrow">ADMIN ANALYTICS</p><h1>Academy reports</h1></div><div className="stats-grid"><article><strong>{users}</strong><span>Users</span></article><article><strong>{courses}</strong><span>Courses</span></article><article><strong>{completed}</strong><span>Course completions</span></article><article><strong>{Math.round(attempts._avg.score||0)}%</strong><span>Average quiz score</span></article><article><strong>{attempts._count}</strong><span>Quiz attempts</span></article><article><strong>{certificates}</strong><span>Certificates issued</span></article></div><section className="card"><h2>Assignment status</h2>{submissions.map(s=><p key={s.status}>{s.status}: <strong>{s._count}</strong></p>)}</section><a className="primary inline" href="/api/admin/reports/users.csv">Export learner CSV</a></Shell>}
