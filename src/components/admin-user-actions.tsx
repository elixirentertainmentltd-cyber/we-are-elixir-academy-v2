'use client'; import { useRouter } from 'next/navigation'; export function AdminUserActions({id,status}:{id:string;status:string}){const router=useRouter();async function setStatus(next:string){await fetch(`/api/admin/users/${id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({status:next})});router.refresh()}return <div className="flex gap-2">{status!=='ACTIVE'&&<button className="small primary" onClick={()=>setStatus('ACTIVE')}>Approve</button>}{status!=='DISABLED'&&<button className="small ghost" onClick={()=>setStatus('DISABLED')}>Disable</button>}</div>}
